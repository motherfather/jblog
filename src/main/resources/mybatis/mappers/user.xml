<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<select id="login" parameterType="map" resultType="uservo">
		<![CDATA[
			select no, name from users where id=#{id } and password=#{password }
		]]>
	</select>
	
	<insert id="join" parameterType="uservo">
	<selectKey keyProperty="no" resultType="Long" order="BEFORE">
		select jblog_seq.nextval from dual
	</selectKey>
		<![CDATA[
			insert into users values(#{no }, #{id }, #{name }, #{password }, sysdate)
		]]>
		<![CDATA[
			insert into users values(#{no }, #{id }, 'logo.jpg')
		]]>
	</insert>
</mapper>