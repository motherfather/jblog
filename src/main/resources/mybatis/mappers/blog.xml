<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">
	<insert id="createBlog" parameterType="long">
		<![CDATA[
			insert into blog values(#{no }, '기본블로그', 'logo.jpg')		
		]]>
	</insert>

	<select id="checkBlog" parameterType="string" resultType="string">
		<![CDATA[
			select id from users where id=#{id }
		]]>
	</select>
	
	<select id="infoBlog" parameterType="string" resultType="blogvo">
		<![CDATA[
			select b.title, b.logo from blog b, users u where b.users_no= u.no and u.id=#{id }
		]]>
	</select>
</mapper>